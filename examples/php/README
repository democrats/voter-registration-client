These PHP examples require:

- PHP 5.2.0 or later
- PHP cURL extention
- PHP JSON extension

That probably means something like:

# apt-get install php5-cli php5-curl php5-json

or

# yum install -y php5 php5-curl php5-json
